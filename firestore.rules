rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder — bloqueia tudo até definirmos collections e RBAC
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
