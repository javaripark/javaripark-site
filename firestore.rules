rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /artifacts/central-de-reservas-jsp/public/data/{collection}/{document=**} {
      allow read, write: if true;
    }
  }
}
